paths:
    migrations: %%PHINX_CONFIG_DIR%%/migrations

environments:
    default_migration_table: phinxlog
    default_database: development
    production:
        adapter: mysql
        host: localhost
        name: easetest
        user: easetest
        pass: 'easetest'
        port: 3306
        charset: utf8

    development:
        adapter: mysql
        host: localhost
        name: easetest
        user: easetest
        pass: easetest
        port: 3306
        charset: utf8

    testing:
        adapter: mysql
        host: localhost
        name: easetest
        user: easetest
        pass: easetest
        port: 3306
        charset: utf8
